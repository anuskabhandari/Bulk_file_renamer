# Bulk File Organizer

This tool renames multiple files in a folder automatically.

## Features
- Add prefix/suffix
- Number files automatically
- Replace words in file names
- Supports different file types

## Usage
Run `main.py` in PyCharm or terminal